---
description:
  type: text
  label: Description
  description: ""
  value: "中文十万行级别压力测试 · 图片 · 视频 · 数学 · 表格 · 注释修订"
author:
  type: text
  label: Author
  description: ""
  value: "Codex"
cover:
  type: asset
  label: Cover Image
  description: ""
  value: "../assets/guides/code-examples-cover-nanobanana.jpg"
avatar:
  type: asset
  label: Avatar
  description: ""
  value: "../assets/nanobanana-avatar.svg"
subject:
  type: text
  label: Subject
  description: ""
  value: "Zditor Stress Test"
title:
  type: text
  label: Title
  description: ""
  value: "中文十万行压力测试"
tags:
  type: text
  label: Tags
  description: ""
  value: "中文 · 压力测试 · 十万行 · 资源 · Markdown"
updated:
  type: date
  label: Updated
  description: ""
  value: "2026-04-23"
line_count:
  type: number
  label: Line Count
  description: ""
  value: 1000
completion:
  type: progress
  label: Completion
  description: ""
  value: 100
display:
  type: checkbox
  label: 显示属性
  description: ""
  value: false
warm:
  type: checkbox
  label: 暖色调
  description: ""
  value: true
col:
  type: array
  label: Col
  description: ""
  value: ["subject", "title", "description"]
row:
  type: array
  label: Row
  description: ""
  value: ["avatar", "author", "updated", "tags", "line_count", "completion"]
---
# 中文十万行压力测试

这是一份面向 Zditor 渲染、滚动、搜索、编辑和资源加载的中文压力测试文档。它刻意达到十万行级别，主体内容允许大量重复，用来观察长文档中的行级解析、块级节点更新、图片缓存、视频节点、附件节点、PDF 卡片、SuperTag、注释与修订等行为[^1]。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::smile:::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```


## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |


> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。


### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。


### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

!!! info 文档目标
    这份文档优先测试编辑器在超长中文文本中的稳定性，而不是追求文学表达。大量重复是刻意设计，便于复现滚动、搜索、折叠、目录和增量渲染问题。

!!! warning 生成说明
    代码块数量保持较少，所有代码围栏都使用三个反引号闭合。主体重复段落不会插入新的代码围栏，避免误伤后续解析。

??? tip 折叠块也参与测试
    这个折叠块用于验证长文档中折叠节点的展开、收起、定位和复制行为。它的内容不长，但会和后续十万行正文一起参与渲染压力。

## 资源节点

![Zditor 中文首页](../assets/home_zh.png|scale=80)

![Transformer 论文封面](../assets/papers/transformer-paper-cover-nanobanana.jpg|scale=58|align=right)

![教父电影封面](../assets/movies/covers/教父.jpg|scale=34|align=left)

[百炼演示视频](../examples/llm_model_critique_talk/bailian-presentation.mp4|mode=video)

[第一页讲稿音频附件](../examples/llm_model_critique_talk/audio_pages/page_01.mp3|mode=attachment|size=602788)

[Attention论文附件](../assets/papers/attention.pdf|mode=attachment|size=2215244)

[attention](../assets/papers/attention.pdf|mode=pdf_card|highlight=fc184920-05e7-4424-84e6-e5be3d46f8df)

## 行为覆盖清单

- [x] 中文长段落与大量重复行
- [x] 图片节点、缩放参数和对齐参数
- [x] 视频节点与附件节点
- [x] PDF 卡片节点
- [x] 表格、任务列表、引用、脚注和数学公式
- [x] `tip` 与 `revision` 内联扩展语法
- [x] Mermaid 与普通代码围栏闭合检查
- [ ] 人工观察滚动、搜索、折叠和复制粘贴表现

## 内联语法样本

这行包含 ==高亮文本==、~~删除线文本~~、内联公式 $O(n \log n)$、表情 :::::::rocket:::::::，以及一个 [虚拟滚动](验证大文档滚动与增量渲染|mode=tip|style=teal) 注释。

这行包含修订建议：把 [尽快处理](时间边界不够明确|mode=revision|style=red|advice=在2026年4月30日前处理) 改成更可执行的说法。

这行引用电影 SuperTag：[教父](./电影/教父.md|mode=supertag)，也引用文档 SuperTag：[Transformer论文解读](./Transformer论文解读.md|mode=supertag)。

> 长文档压力测试不只看首屏速度，也要看跳转、搜索命中、选择大段文本、撤销重做、滚动恢复和资源懒加载是否稳定。

## 表格样本

|模块 |样例 |观察点 |
|---|---|---|
|图片 |`scale=80`、`align=center` |首次加载、缓存复用、滚动回收 |
|视频 |`mode=video` |媒体卡片尺寸、懒加载、播放入口 |
|附件 |`mode=attachment` |文件大小展示、链接解析 |
|PDF |`mode=pdf_card` |高亮 ID、侧边信息、降级展示 |
|表格换行 |`第一行\n第二行` |单元格内换行渲染 |

## 数学样本

$$
T(n)=aT\left(
\frac{n}{b}
\right)+f(n)
$$

$$
\operatorname{latency}_{p95}=\operatorname{median}(x)+1.645\cdot\sigma
$$

## Mermaid 样本

```mermaid
flowchart LR
  A[打开文档] --> B[解析 Frontmatter]
  B --> C[渲染资源节点]
  C --> D[滚动十万行正文]
  D --> E[搜索与定位]
  E --> F[观察内存与交互]
```

## 短代码块样本

```bash
zditor-open 中文/十万行压力测试.md
```

```json
{"purpose":"stress-test","lines":100000,"language":"zh-CN","assets":["image","video","pdf","audio"]}
```

```sql
select section, count(*) as rows
from zditor_stress_log
group by section
order by rows desc;
```

## 较长代码块样本一

```py
from __future__ import annotations

from dataclasses import dataclass
from statistics import mean
from typing import Iterable, Iterator


@dataclass(frozen=True)
class ScrollSample:
    index: int
    offset: int
    render_ms: float
    visible_nodes: int
    cached_nodes: int


@dataclass(frozen=True)
class ScrollReport:
    total: int
    average_ms: float
    max_ms: float
    cache_hit_hint: float


def windowed(values: list[ScrollSample], size: int) -> Iterator[list[ScrollSample]]:
    if size <= 0:
        raise ValueError("size must be positive")
    for start in range(0, len(values), size):
        yield values[start:start + size]


def summarize(samples: Iterable[ScrollSample]) -> ScrollReport:
    data = list(samples)
    if not data:
        return ScrollReport(total=0, average_ms=0.0, max_ms=0.0, cache_hit_hint=0.0)
    render_times = [item.render_ms for item in data]
    visible_total = sum(item.visible_nodes for item in data) or 1
    cached_total = sum(item.cached_nodes for item in data)
    return ScrollReport(
        total=len(data),
        average_ms=mean(render_times),
        max_ms=max(render_times),
        cache_hit_hint=cached_total / visible_total,
    )


def build_samples(total: int) -> list[ScrollSample]:
    samples: list[ScrollSample] = []
    for index in range(total):
        samples.append(
            ScrollSample(
                index=index,
                offset=index * 42,
                render_ms=8.0 + (index % 17) * 0.37,
                visible_nodes=40 + index % 9,
                cached_nodes=28 + index % 11,
            )
        )
    return samples


def main() -> None:
    samples = build_samples(120)
    for batch in windowed(samples, 30):
        report = summarize(batch)
        print(
            f"batch={report.total}, avg={report.average_ms:.2f}ms, "
            f"max={report.max_ms:.2f}ms, cache={report.cache_hit_hint:.2f}"
        )


if __name__ == "__main__":
    main()
```

## 较长代码块样本二

```ts
type AssetKind = "image" | "video" | "attachment" | "pdf";

type AssetNode = {
  id: string;
  kind: AssetKind;
  path: string;
  visible: boolean;
  loaded: boolean;
};

type RenderEvent = {
  line: number;
  action: "mount" | "update" | "unmount";
  durationMs: number;
};

class StressViewport {
  private assets = new Map<string, AssetNode>();
  private events: RenderEvent[] = [];

  register(asset: AssetNode): void {
    this.assets.set(asset.id, asset);
  }

  markVisible(id: string, visible: boolean): void {
    const asset = this.assets.get(id);
    if (!asset) return;
    this.assets.set(id, { ...asset, visible, loaded: asset.loaded || visible });
  }

  record(event: RenderEvent): void {
    this.events.push(event);
  }

  get loadedAssets(): AssetNode[] {
    return [...this.assets.values()].filter((asset) => asset.loaded);
  }

  get slowEvents(): RenderEvent[] {
    return this.events.filter((event) => event.durationMs > 16.7);
  }

  summary(): string {
    const total = this.events.length;
    const slow = this.slowEvents.length;
    const loaded = this.loadedAssets.length;
    return `events=${total}; slow=${slow}; loadedAssets=${loaded}`;
  }
}

const viewport = new StressViewport();

viewport.register({
  id: "home-zh",
  kind: "image",
  path: "../assets/home_zh.png",
  visible: false,
  loaded: false,
});

viewport.register({
  id: "demo-video",
  kind: "video",
  path: "../examples/llm_model_critique_talk/bailian-presentation.mp4",
  visible: false,
  loaded: false,
});

for (let line = 1; line <= 100000; line += 2500) {
  viewport.record({ line, action: "update", durationMs: 7 + (line % 31) });
  if (line === 5000) viewport.markVisible("home-zh", true);
  if (line === 15000) viewport.markVisible("demo-video", true);
}

console.log(viewport.summary());
```

## 场景 01：首屏打开

验证文档标题、Frontmatter 属性、资源节点和目录入口在初次打开时是否稳定。
首屏内容应保持完整，图片和媒体卡片可以逐步加载，但不应影响文本阅读。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |首屏打开 场景中的中文段落、列表和引用。 |
|资源 |检查与 首屏打开 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 首屏打开 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：首屏打开 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 02：目录跳转

验证长文档中的标题层级是否可以被目录准确识别，并在点击后定位到正确位置。
目录滚动时需要保持选中状态清晰，避免跳转后停留在错误段落。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |目录跳转 场景中的中文段落、列表和引用。 |
|资源 |检查与 目录跳转 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 目录跳转 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：目录跳转 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 03：资源懒加载

验证图片、视频、音频附件和 PDF 卡片在进入视口前后的加载时机。
资源节点允许延迟加载，但占位高度和链接信息应该保持稳定。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |资源懒加载 场景中的中文段落、列表和引用。 |
|资源 |检查与 资源懒加载 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 资源懒加载 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：资源懒加载 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 04：搜索定位

验证中文关键词、英文关键词、路径片段和数字内容在长文档中的搜索表现。
搜索结果应支持连续跳转，并在返回编辑区后保留当前光标位置。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |搜索定位 场景中的中文段落、列表和引用。 |
|资源 |检查与 搜索定位 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 搜索定位 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：搜索定位 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 05：文本选择

验证从普通段落跨越列表、引用、表格和数学块时的选择行为。
选择区域需要可见且连续，复制结果应保留 Markdown 语义。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |文本选择 场景中的中文段落、列表和引用。 |
|资源 |检查与 文本选择 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 文本选择 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：文本选择 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 06：块级更新

验证在文档中部插入、删除和移动块节点时，周围内容是否正确刷新。
增量更新不应造成标题丢失、列表错位或资源节点重复挂载。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |块级更新 场景中的中文段落、列表和引用。 |
|资源 |检查与 块级更新 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 块级更新 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：块级更新 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 07：表格编辑

验证表格列宽、单元格换行、内联代码和中文标点混排。
表格编辑时应保持光标移动自然，单元格内容不应吞掉相邻分隔符。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |表格编辑 场景中的中文段落、列表和引用。 |
|资源 |检查与 表格编辑 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 表格编辑 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：表格编辑 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 08：数学渲染

验证行内公式和块级公式在滚动过程中的尺寸计算与重新排版。
公式加载慢时可以显示占位，但不应造成页面突然大幅跳动。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |数学渲染 场景中的中文段落、列表和引用。 |
|资源 |检查与 数学渲染 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 数学渲染 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：数学渲染 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 09：代码围栏

验证 Mermaid、Bash、JSON、SQL、Python 和 TypeScript 围栏的闭合状态。
代码块内部的特殊字符不应影响后续 Markdown 段落解析。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |代码围栏 场景中的中文段落、列表和引用。 |
|资源 |检查与 代码围栏 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 代码围栏 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：代码围栏 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 10：注释修订

验证 tip、revision、supertag 等扩展链接在长文档中的解析和编辑。
扩展节点应能被复制、删除和撤销，并保持参数不被意外改写。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |注释修订 场景中的中文段落、列表和引用。 |
|资源 |检查与 注释修订 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 注释修订 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：注释修订 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 11：撤销重做

验证连续输入、批量粘贴、删除段落和移动块后的撤销栈。
撤销重做需要恢复文本、选择区域和资源节点状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |撤销重做 场景中的中文段落、列表和引用。 |
|资源 |检查与 撤销重做 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 撤销重做 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：撤销重做 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 12：滚动恢复

验证关闭后重新打开文档时，编辑器是否恢复到上次阅读位置。
恢复位置应靠近原段落，避免跳回首屏或落到空白区域。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |滚动恢复 场景中的中文段落、列表和引用。 |
|资源 |检查与 滚动恢复 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 滚动恢复 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：滚动恢复 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 13：移动端视口

验证窄屏下标题、表格、代码块和附件卡片是否保持可读。
移动端可以横向滚动表格，但正文段落不应溢出屏幕。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |移动端视口 场景中的中文段落、列表和引用。 |
|资源 |检查与 移动端视口 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 移动端视口 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：移动端视口 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 14：键盘操作

验证方向键、分页键、快捷键和输入法组合在长文档中的响应。
键盘导航应与鼠标滚动共享同一份定位状态。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |键盘操作 场景中的中文段落、列表和引用。 |
|资源 |检查与 键盘操作 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 键盘操作 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：键盘操作 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 15：粘贴导入

验证从外部复制富文本、纯文本和 Markdown 片段后的解析结果。
粘贴内容应遵循当前文档语法，不应产生未闭合代码块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |粘贴导入 场景中的中文段落、列表和引用。 |
|资源 |检查与 粘贴导入 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 粘贴导入 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：粘贴导入 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 16：批注阅读

验证带批注的链接在普通阅读、编辑光标和预览状态之间的表现。
批注弹层应靠近触发文本，滚动时应自动收起或重新定位。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |批注阅读 场景中的中文段落、列表和引用。 |
|资源 |检查与 批注阅读 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 批注阅读 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：批注阅读 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 17：PDF 卡片

验证 PDF 附件卡片和高亮标识在长文档中的定位与回显。
PDF 卡片失败时应退化为可点击附件，而不是空白块。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |PDF 卡片 场景中的中文段落、列表和引用。 |
|资源 |检查与 PDF 卡片 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> PDF 卡片 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：PDF 卡片 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 18：视频节点

验证视频卡片的封面、播放入口、尺寸和暂停状态。
视频不应在滚动经过时自动抢焦点，也不应阻塞文本输入。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |视频节点 场景中的中文段落、列表和引用。 |
|资源 |检查与 视频节点 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 视频节点 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：视频节点 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 19：导出检查

验证导出 Markdown、HTML 或其他格式时，扩展语法是否保留。
导出结果应包含资源路径、表格、公式、引用和脚注。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |导出检查 场景中的中文段落、列表和引用。 |
|资源 |检查与 导出检查 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 导出检查 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：导出检查 场景用于补充基础样本之外的真实阅读和编辑路径。

## 场景 20：综合验收

验证从首屏到文末的完整阅读路径，以及搜索、编辑、撤销的组合操作。
综合测试更接近真实使用，可以暴露单项测试看不到的问题。

### 观察重点

- 滚动过程中标题锚点保持可点击。
- 编辑区和预览区的块级位置保持一致。
- 资源加载失败时显示明确的降级信息。
- 连续输入时不出现明显卡顿或光标漂移。

### 操作步骤

1. 打开文档并等待首屏内容稳定。
2. 跳转到本节标题附近，观察滚动位置。
3. 修改一小段文字，再执行撤销和重做。
4. 搜索本节关键词，确认命中高亮和返回位置。

### 数据记录

|项目 |记录 |
|---|---|
|文本 |综合验收 场景中的中文段落、列表和引用。 |
|资源 |检查与 综合验收 相关的图片、附件或媒体卡片。 |
|交互 |记录点击、输入、搜索、拖选和撤销表现。 |
|风险 |关注滚动跳动、节点错位、内容丢失和状态不同步。 |

> 综合验收 场景完成后，需要回到目录或搜索结果，确认定位状态没有被前一次操作污染。

结论：综合验收 场景用于补充基础样本之外的真实阅读和编辑路径。

## 附录：长文档人工验收记录

这一节用于记录人工观察结果，避免压力测试只剩下机械行号。
验收时可以按场景逐项填写，也可以只记录出现异常的章节。

### 验收清单

- 首屏标题、属性和资源节点显示完整。
- 目录跳转、搜索跳转和浏览器返回位置一致。
- 表格、数学公式、代码块和引用没有破坏后续解析。
- 图片、视频、音频附件和 PDF 卡片可以按需加载。
- 撤销、重做、复制和粘贴不会造成内容丢失。

### 异常记录模板

|字段 |写法 |
|---|---|
|时间 |记录到分钟 |
|位置 |章节名或搜索词 |
|操作 |点击、输入、拖选或粘贴 |
|现象 |截图、录屏或一句话描述 |
|结论 |已复现、偶发或未复现 |

> 这个附录用于替代纯粹的行号堆叠，让一千行文档仍然像真实说明文一样可读。

更新记录：2026-04-23 将行级样本改为正常文档扩展。
校验记录：文件总行数保持为 1000 行。
完。

[^1]: 长文档压力测试可以包含重复结构，但正文仍应保持章节、段落、清单、表格和引用等正常文档形态。
